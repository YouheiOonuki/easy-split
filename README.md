# easy-split — 割り勘・傾斜清算アプリ

飲み会や食事会の清算をスマホで素早く行うための「割り勘＋傾斜清算」Web アプリです。

## 機能一覧

| 機能 | 説明 |
|------|------|
| **割り勘計算** | 合計金額を参加者で均等に分割 |
| **傾斜清算（係数割り）** | 各参加者に 0.5〜2.0 の係数を設定して差をつけた清算 |
| **幹事持ち出しゼロモード** | 幹事の支払い額を指定、残額を他メンバーで自動配分 |
| **QR コード生成** | 各参加者の支払い額を QR コードで共有 |
| **テーマ切り替え** | Dark / Simple / Metal の3テーマ |
| **履歴機能** | 過去5回の計算結果を保存（ON/OFF 可能） |
| **名前の自動補完** | 過去に入力した参加者名を記憶 |
| **デフォルト値** | 初回アクセス時にサンプルデータで即体験可能 |

## スマホでの使い方

1. ブラウザで easy-split を開く
2. **合計金額**を入力（例：10,000円）
3. **参加者**を追加し、名前を入力
4. 必要に応じて**係数スライダー**で傾斜を設定
5. **「計算する」**ボタンをタップ
6. 各参加者の支払い額が表示される
7. QR コードで支払い額を共有

### 幹事モードの使い方

1. 「幹事の持ち出しゼロモード」を ON にする
2. 幹事を選択し、幹事の支払い額を入力（例：0円）
3. 計算すると、残額が他の参加者に自動配分される

## 技術構成

- **HTML / CSS / JavaScript** のみ（フレームワーク不使用）
- **データ保存**: localStorage のみ（サーバー通信なし）
- **QR コード**: [qrcode.js](https://github.com/davidshimjs/qrcodejs) (CDN)
- **テーマ**: CSS 変数 + `data-theme` 属性

## GitHub Pages 公開手順

1. GitHub にリポジトリをプッシュ
2. **Settings** → **Pages** を開く
3. Source で **Deploy from a branch** を選択
4. Branch を **main** (または公開したいブランチ) / **/ (root)** に設定
5. **Save** をクリック
6. 数分後に `https://<username>.github.io/easy-split/` で公開

## SEO 対策

- `<title>` / `<meta description>` / `<meta keywords>` を設定済み
- **OGP** (Open Graph Protocol) タグを設定済み
- **JSON-LD** 構造化データ (`WebApplication`) を埋め込み済み
- ファビコン (SVG / ICO / PNG) を設定済み

## Google AdSense 対策

AdSense 審査に通過しやすい構成を採用しています。

- **About / 利用規約 / プライバシーポリシー** ページを完備
- 各ページに十分なテキストコンテンツ
- 広告枠は `<!-- AD_PLACEHOLDER -->` コメントで配置済み
- 審査通過後にコメントを AdSense コードに置き換えてください

## 今後の拡張予定

- **PWA 対応** — オフラインでも使えるように Service Worker を導入
- **多言語対応** — 英語版の追加
- **送金リンク生成** — PayPay / LINE Pay などの送金リンク連携
- **グループ管理** — よく使うメンバーをグループとして保存
- **レシート読み取り** — カメラでレシートを読み取って金額を自動入力

## ライセンス

MIT License

## 開発者

**Youhei**
